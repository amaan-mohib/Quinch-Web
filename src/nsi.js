import React from 'react';

export default function Nsi(){
    return(
        <div id="game" className="raleway" style={{'fontSize':'20px', 'textAlign':'center', 'padding':'20px'}}>
            Sign in to keep track of your highscores and to access settings.
        </div>
    );
}